# AI Debugging

## Trace IDs
- Trace IDs are generated per run and propagated across services
- Use trace ID to correlate run steps, tool runs, and audit events

## Local Debugging
- Inspect run records and run steps in the database
- Use Playwright traces for E2E failures
